# google-adk-agent 

- This is a simple AI agent that runs on Google Cloud Shell and uses Vertex AI API to handle chat conversation

## Quick Start

```bash
# Clone & install
git clone https://github.com/teehere/google-adk-agent.git
cd google-adk-agent
pip install -r requirements.txt

# Authenticate with Google Cloud
gcloud auth application-default login
